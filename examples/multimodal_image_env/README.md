# Multimodal Image Environment

Tiny image-bearing Freesolo environment for Flash SFT/GRPO smoke tests.

Upload it with:

```bash
flash env push --name image-color examples/multimodal_image_env
```

Then paste the returned environment id into `configs/sft.toml` or `configs/rl.toml`.
