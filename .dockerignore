# the Dockerfile is single-stage (COPY . .), so there is no later stage to drop this
# again: an ordinary clone would otherwise bake every object and reflog entry (including
# any history a scrub was meant to remove) into an image layer.
.git
.venv/
venv/
__pycache__/
*.pyc
.ruff_cache/
.cache/
.flash/
results/
hf_cache/
uv-cache/
dist/
build/*
# ...but keep the opt-in kernel-cache staging dir in the build context: Dockerfile.worker
# copies build/kernel_cache/ (the .keep placeholder makes the source always exist; the
# gpu-produced mega_cache.bin + mega_cache.json ride along on a kernel-cache bake).
!build/kernel_cache/
!build/kernel_cache/**
*.egg-info/
# the Dockerfile does COPY . ., so an operator's .env.local would otherwise become an
# image layer even though it was never committed. .env.example carries no values.
.env
.env.*
!.env.example
.flash/
