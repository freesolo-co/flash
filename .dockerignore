.venv/
venv/
__pycache__/
*.pyc
.ruff_cache/
.cache/
.flash/
results/
hf_cache/
uv-cache/
dist/
build/
# ...but keep the opt-in kernel-cache staging dir in the build context: Dockerfile.worker
# COPYs build/kernel_cache/ (the .keep placeholder makes the source always exist; the
# GPU-produced mega_cache.bin rides along only on a --build-arg BUILD_KERNEL_CACHE=true bake).
!build/kernel_cache/
*.egg-info/
.env
.flash/
